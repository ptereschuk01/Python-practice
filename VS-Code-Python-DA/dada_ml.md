# ✅ data_ml — Environment Check-Up

Этот файл — **чек-лист для проверки и восстановления ML-окружения `data_ml`** в Anaconda (Windows).

---

## 🧩 1. requirements.txt (базовый набор)

Минимальный и достаточный набор библиотек для:

* EDA
* визуализации
* ML-моделей
* работы в Jupyter

```txt
numpy
pandas
matplotlib
seaborn
scikit-learn
scipy
jupyter
ipykernel
```

---

## 🖥️ 2–8. Проверка и настройка окружения (в виде таблицы)

Ниже — **человеко-понятный чек-лист**: что вводить в Anaconda Prompt и зачем это нужно.

| Команда                                                                                                                                                                                                                                                                                                                                                                                                                | Описание                                                                                                                                                           |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `conda activate data_ml`                                                                                                                                                                                                                                                                                                                                                                                               | Активируем нужное окружение. Все дальнейшие команды работают **только после этого**. В строке должно появиться `(data_ml)`.                                        |
| `where python`                                                                                                                                                                                                                                                                                                                                                                                                         | Проверяем, что используется Python именно из окружения `data_ml`, а не из `base`. Путь должен вести в `anaconda3\\envs\\data_ml`.                                  |
| `python -c "import numpy; print('numpy OK')" && python -c "import pandas; print('pandas OK')" && python -c "import matplotlib; print('matplotlib OK')" && python -c "import seaborn; print('seaborn OK')" && python -c "import sklearn; print('sklearn OK')" && python -c "import scipy; print('scipy OK')" && python -c "import jupyter; print('jupyter OK')" && python -c "import ipykernel; print('ipykernel OK')"` | Быстрая проверка всех основных библиотек: numpy, pandas, matplotlib, seaborn, scikit-learn, scipy, jupyter, ipykernel. Если видишь `OK` после каждой — всё готово. |
| `conda install numpy pandas matplotlib seaborn scikit-learn scipy jupyter ipykernel`                                                                                                                                                                                                                                                                                                                                   | Устанавливаем **всё необходимое сразу** через conda (рекомендуемый способ).                                                                                        |
| `python -m pip install numpy pandas matplotlib seaborn scikit-learn scipy jupyter ipykernel`                                                                                                                                                                                                                                                                                                                           | Запасной вариант, если conda зависает или не может решить зависимости.                                                                                             |
| `python -m ipykernel install --user --name data_ml --display-name "Python (data_ml)"`                                                                                                                                                                                                                                                                                                                                  | Регистрируем kernel, чтобы Jupyter Notebook **видел окружение `data_ml`**.                                                                                         |
| `python -c "import seaborn, pandas, sklearn; print('ALL OK 🚀')"`                                                                                                                                                                                                                                                                                                                                                      | Финальная проверка: если видишь `ALL OK 🚀`, окружение полностью готово к работе.                                                                                  |

---

## 🧯 Частые проблемы

### ❓ Почему `data_ml` отображается два раза в `conda env list`?

Это **одно и то же окружение**. Разница в регистре пути (`C:` vs `c:`).

### ❓ В Jupyter нет seaborn, а в prompt есть?

Значит выбран **не тот kernel**. См. шаг с `ipykernel`.

---

## 📌 Рекомендация

После стабилизации окружения:

```bash
pip freeze > requirements.txt
```

Используй этот файл для GitHub и воспроизводимости проекта.

---

✅ **Этот файл можно хранить в корне проекта как `data_ml_CheckUp.md`.**

